%h1 Listing firms

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - @firms.each do |firm|
      %tr
        %td= link_to 'Show', firm
        %td= link_to 'Edit', edit_firm_path(firm)
        %td= link_to 'Destroy', firm, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Firm', new_firm_path
